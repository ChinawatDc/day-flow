# day-flow — User flows

## เข้าใช้
1. เปิดเว็บบนมือถือ (หรือ PWA)
2. `/login` สมัครหรือเข้าด้วยเมล+รหัส
3. ไปบ้าน `/menu` (`/` redirect มาที่นี่)
4. มือถือแท็บล่าง: วันนี้ / จด / **เมนู** / เงิน / งาน
5. เดสก์ท็อป: rail ซ้ายครบบท รวมตั้งค่า

## เมนู (บ้าน)
1. สวัสดี + ความคืบหน้างาน + ยอดวันนี้
2. เปิดบทอื่นจากกริดโมดูล
3. การ์ดวันนี้พาไป `/today` สำหรับสรุปรายวัน

## วันนี้
1. เห็นยอดจ่าย งานวันนี้ งานค้างข้ามวัน จดค้าง บิลเดือนนี้ คลังใกล้หมด
2. จดรายจ่ายหรือติ๊กงานได้ในหน้า โดยไม่ผ่านฮับ
3. ค้นจากกล่องบนบ้าน → เปิดบทต้นทาง
4. คัดลอกบิลเดือนที่แล้วได้ (ข้ามชื่อที่เดือนนี้มีแล้ว)

## จดด่วน → จัดเข้าโมดูล
1. `/inbox` พิมพ์หรือแนบไฟล์ → รายการ `unfiled`
2. กด งาน / คลัง / บ้าน / บันทึก / ทิ้ง หรือกรอกจำนวนแล้วบันทึกเป็นเงิน
3. หลังจัด กลับบ้านวันนี้

## ไฟล์
ฟอร์มที่มีไฟล์ → Server Action อัปโหลด R2 private → เปิดผ่าน `/api/files?key=` เฉพาะ key ที่ขึ้นต้น `userId/` หรือ `family/{familyId}/` เมื่อเป็นสมาชิกบ้าน

## เลือกบ้าน (ครอบครัว)
1. `/family` แท็บบ้าน → การ์ด **เลือกบ้าน**
2. `/family/hunt` ตาราง + กรองงบ / บ้านเดี่ยวเท่านั้น
3. กดโครงการ → รายละเอียด โหวต โน้ต นัดดู
4. เทียบได้ 2–4 ตัว · shortlist ร่วม · นัดดูลงปฏิทินครอบครัว
5. LINE (ถ้าตั้งค่า): เข้าใช้ / ผูกในตั้งค่า · OA ส่งการ์ดเมื่อคู่เก็บหรือนัดดู
