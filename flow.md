# day-flow — User flows

## เข้าใช้
1. เปิดเว็บบนมือถือ (หรือ PWA)
2. `/login` สมัครหรือเข้าด้วยเมล+รหัส
3. `/` ฮับเมนูกระเบื้องไม่เท่ากัน
4. กดโมดูล → ทำงานในหน้านั้น
5. มือถือ: แท็บล่าง ฮับ / วันนี้ / จด / เงิน + แผ่น «อื่นๆ»
6. เดสก์ท็อป: rail ซ้าย

## จดด่วน → จัดเข้าโมดูล
1. `/inbox` พิมพ์หรือแนบไฟล์ → รายการ `unfiled`
2. กด งาน / เงิน / คลัง / บ้าน / บันทึก / ทิ้ง
3. งาน·คลัง·บ้าน·บันทึก สร้างเรคคอร์ดให้อัตโนมัติ
4. **เงิน** ตอน V0.0.1 แค่ติด kind — ต้องกรอกจำนวนที่ `/money` เอง (ปิดช่องนี้ใน V0.0.4)

## วันนี้
อ่านสรุปจากโมดูลอื่น: ยอดจ่ายวันนี้, Inbox ค้าง, งานครบวันนี้, มีบันทึกวันนี้หรือยัง, เอกสารใกล้หมดอายุ

## ไฟล์
ฟอร์มที่มีไฟล์ → Route ไม่ได้ ใช้ Server Action อัปโหลด R2 private → เปิดผ่าน `/api/files?key=` (signed URL) เฉพาะ key ที่ขึ้นต้น `userId/`
